```
npm i -g anydoor-liusisi
```

## 使用方法
```
anydoor-liusisi # 把当前文件夹作为静态资源服务器根目录

anydoor-liusisi -p 8080 # 设置端口号为 8080

anydoor-liusisi -h localhost # 设置 host 为 localhost

anydoor-liusisi -d /usr # 设置根目录为根目录
```